<!--pages/recipe/recipe.wxml-->
<view class="recipe-page">
  <!-- 侧边分类栏 -->
  <scroll-view scroll-y class="sidebar">
    <view 
      wx:for="{{groups}}" 
      wx:key="groupId"
      class="sidebar-item {{selectedGroupId === item.groupId ? 'active' : ''}}"
      bindtap="selectGroup"
      data-group-id="{{item.groupId}}"
    >
      <text class="sidebar-text">{{item.groupName}}</text>
      <view wx:if="{{selectedGroupId === item.groupId}}" class="active-indicator"></view>
    </view>
  </scroll-view>

  <!-- 右侧内容区 -->
  <view class="content-area">
    <!-- 食谱列表 -->
    <scroll-view scroll-y class="recipe-list" wx:if="{{!loading && recipes.length > 0}}">
      <view class="recipe-grid">
        <view 
          wx:for="{{recipes}}" 
          wx:key="id"
          class="recipe-card recipe-card-bg-{{item.bgIndex}}"
          bindtap="goToDetail"
          data-recipe-id="{{item.id}}"
          data-bg-index="{{item.bgIndex}}"
        >
          <view class="recipe-content-card">
              <view class="recipe-name">{{item.name}}</view>
              <view class="recipe-promotion">{{item.promotion}}</view>
              <!-- 图片容器 -->
              <view class="recipe-image-container">
                <image class="recipe-image" src="{{item.picUrl}}" mode="aspectFill" />
              </view>
            </view>
        </view>
      </view>
    </scroll-view>

    <!-- 加载中 -->
    <view class="loading-state" wx:if="{{loading}}">
      <van-loading type="spinner" size="48rpx" color="#E9503F">加载中...</van-loading>
    </view>

    <!-- 空状态 -->
    <view class="empty-state" wx:if="{{!loading && showEmpty}}">
      <text class="empty-text">还没有收藏的食谱</text>
      <text class="empty-hint">快去收藏你喜欢的食谱吧~</text>
    </view>

    <!-- 普通空状态 -->
    <view class="empty-state" wx:if="{{!loading && !showEmpty && recipes.length === 0}}">
      <text class="empty-icon">🍽️</text>
      <text class="empty-text">暂无食谱</text>
    </view>
  </view>
</view>

