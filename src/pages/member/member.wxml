<!-- pages/member/member.wxml -->
<view class="member-page">
  <view class="main-card">
    <!-- 会员卡片 -->
    <view class="header-section">
      <view class="vip-card-box">
        <view class="vip-card">
          <view class="card-bg-mesh"></view>
          
          <view class="card-body">
            <view class="brand-line">
              <view class="h-line"></view>
              <text class="brand">番茄控卡会员</text>
              <view class="h-line"></view>
            </view>
            
            <view class="card-status-label" wx:if="{{isMember}}">
              <text class="date">有效期至 {{memberExpireAt}}</text>
            </view>
            <text class="card-title" wx:else>未激活</text>

            <view class="card-stats">
              <view class="stat">
                <text class="num">2000+</text>
                <text class="txt">食物推荐</text>
              </view>
              <view class="sep"></view>
              <view class="stat">
                <text class="num">AI</text>
                <text class="txt">智能分析</text>
              </view>
              <view class="sep"></view>
              <view class="stat">
                <text class="num">PRO</text>
                <text class="txt">数据周报</text>
              </view>
            </view>
          </view>

          <view class="card-footer" wx:if="{{!isMember}}">
            <text class="expiry">加入会员，解锁以上全部功能</text>
          </view>
        </view>
      </view>
    </view>

    <view class="content-container">
      <!-- 套餐选择 -->
      <view class="section-card">
        <view class="plan-list">
          <view 
            class="plan-item {{selectedProductId === item.id ? 'selected' : ''}}" 
            wx:for="{{products}}" 
            wx:key="id"
            bindtap="selectProduct"
            data-id="{{item.id}}"
          >
            <view class="plan-tag" wx:if="{{item.recommend}}">推荐</view>
            <text class="plan-name">{{item.name}}</text>
            <view class="plan-price">
              <text class="symbol">¥</text>
              <text class="amount">{{item.price}}</text>
            </view>
            <text class="plan-daily">折合 ¥{{item.dailyPrice}}/天</text>
          </view>
        </view>
      </view>

      <!-- 支付按钮 -->
      <button class="pay-button" bindtap="onBuyMember" disabled="{{isSubmitting}}" wx:if="{{!isIOS}}">
        立即开通 ¥{{selectedProductPrice}}
      </button>
      
      <!-- iOS 客服引导按钮 -->
      <button 
        class="pay-button ios-contact-btn" 
        open-type="contact" 
        wx:if="{{isIOS}}"
      >
        联系客服开通
      </button>
    </view>
  </view>
</view>
